name: Workflow 1

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Repo
              uses: actions/checkout@v4
            - name: Echo
              run: echo "My First Github Actions Workflow"
            - name: Print File
              run: cat README.md
            - name: List Files
              run: ls